<% stylesheets 'convenor.css' %>

<% if tutor_projects.size < 3 %>
	
	<% tutor_projects.each_with_index do |tutor_project_template, i| %>
		<div class="row-fluid">
			<div class="span12 widget">
				<h2><i class="icon-book"></i><%= tutor_project_template.name %></h2>
				<div class="widget-content">
					<%=
						render partial: 'tutor_project',
						locals: { tutor_project_template: tutor_project_template }
					%>
				</div>
			</div>
		</div>
	<% end %>

<% else %>
	<% tutor_projects.each_with_index do |tutor_project_template, i| %>
		<% if i % 2 == 0 %>
		<div class="row-fluid">
		<% end %>
			<div class="span6 widget">
				<h2><i class="icon-book"></i><%= tutor_project_template.name %></h2>
				<div class="widget-content">
					<%=
						render partial: 'tutor_project',
						locals: { tutor_project_template: tutor_project_template }
					%>
				</div>
			</div>
		<% if i % 2 == 1 %>
		</div>
		<% end %>
	<% end %>
<% end %>