<% if student_projects.size < 3 %>
	<% student_projects.each_with_index do |project, i| %>
		<div class="row-fluid">
			<div class="span12 widget project-overview">
				<h2><i class="icon-book"></i><%= project.project_template.name %></h2>
				<div class="widget-content">
					<%=
						render partial: 'student_project',
						locals: { project: project }
					%>
				</div>
			</div>
		</div>
	<% end %>
<% else %>
	<% student_projects.each_with_index do |project, i| %>
		<% if i % 2 == 0 %>
		<div class="row-fluid">
		<% end %>
			<div class="span6 widget project-overview">
				<h2><i class="icon-book"></i><%= project.project_template.name %></h2>
				<div class="widget-content">
					<%=
						render partial: 'student_project',
						locals: { project: project }
					%>
				</div>
			</div>
		<% if i % 2 == 1 %>
		</div>
		<% end %>
	<% end %>
<% end %>