<%= render 'layouts/projects_list', :projects => @projects, :selected_project => @project %>
<div class="span10">
	<div class="row-fluid">
		<ul class="nav nav-pills">
			<li class="active"><%= link_to "Summary", project_path(@project) %></li>
			<li><%= link_to "Tasks", project_tasks_path(@project) %></li>
		</ul>
	</div>
	<div class="row-fluid">
		<div class="span6 widget">
			<%= render 'layouts/activity_stream' %>
		</div>
		<div class="span4 widget">
			<h2>Progress</h2>
			<div class="widget-content" id="progress-widget">
				<%= render 'dashboard/project_progress', :project => @project %>
			</div>
		</div>
	</div>
</div>

<%= javascript_tag do %>
	window.projectURL = "fuckoff";
<% end %>