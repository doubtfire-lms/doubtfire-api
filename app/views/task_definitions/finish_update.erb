// Build the row id string from the task_definition
var taskRowId = "task-" + "<%= @task_definition.id %>" +  "-row";

// Replace the row with the partial in _table_row.html.erb.
$("#" + taskRowId).replaceWith("<%=j render 'task_definitions/table_row', task_definition:  @task_definition %>");