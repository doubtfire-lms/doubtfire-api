<% title "Edit Profile - #{@user.name} (#{@user.username})" %>

<%- model_class = @user.class -%>
<div class="span7 widget">
	<h2><i class="icon-cogs"></i>Your Profile</h2>
	<div class="widget-content">
		<%= render :partial => 'form' %>
	</div>
</div>
