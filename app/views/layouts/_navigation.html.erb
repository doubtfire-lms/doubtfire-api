<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
</a>
<%= link_to "Doubtfire", root_path, :class => 'brand' %>
<% if user_signed_in? %>
<div class="btn-group pull-right">
    <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
      <i class="icon-user"></i> <%= current_user.full_name %> (<%= current_user.username %>)
      <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to 'Profile', edit_profile_path %></li>
      <li class="divider"></li>
      <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
    </ul>
</div>
<% end %>
<div class="nav-collapse">
    <ul class="nav">
        <% if user_signed_in? %>
            <li><%= link_to 'Home', root_path %></li>
            <% if not current_user.superuser? # Don't show "Edit account" for superuser %>
            <li><%= link_to 'Settings', edit_profile_path %></li>
            <% end %>
            <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
        <% end %>
    </ul>
</div>