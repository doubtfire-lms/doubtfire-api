function updateTaskStatusElements() {
	var taskContainer 	= $("#task-"+<%= @task.id %>+"-entry");
	changeStatusLabel(taskContainer, "<%= @task.task_status.name %>");
};

function changeStatusLabel(taskContainer, status) {
	taskContainer.find(".task-status-button").text(status);
}

updateTaskStatusElements();