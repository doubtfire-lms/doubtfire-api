// Build the row id string from the task_template
var taskRowId = "task-" + "<%= @task_template.id %>" +  "-row";

// Replace the row with the partial in _table_row_edit.html.erb.
$("#" + taskRowId).replaceWith("<%=j render :partial => 'task_templates/table_row_edit', :locals => { :task_template => @task_template } %>");