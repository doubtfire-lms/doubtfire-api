var partial = "<%=j render :partial => 'users/table_row_existing_user' %>"

// Render the table_row_edit partial. 
$("#users-table").append(partial);	

// Make sure the table is visible and the "no units" message is hidden.
if($("#project-templates-table").find("tbody").children().length > 0)
{
	$("#no-project-templates-message").hide();
	$("#project-templates-table").show();
}